import { IPeriodSelectionModalData } from './PeriodSelectionModal.types';
/**
 *  @link /Users/shlomoglik/dev/b-cont-san/src/views/commons/confirmBlock/MonthSelect.js
 *  */
export const PeriodSelectionModal = (props: IPeriodSelectionModalData) => {
  /**
   * TODO:
   * need to get via props = current account period , type of account period , projects/id/confirmsSettings
   * from that point need to prompt available options...
   * when user select a period need to create an account with projects/id/confirmSetting as template
   */
  return <>TODO: select a period</>;
};
