import { Card } from '@/lib/components/commons/Card';
import { IProjectAppartmentsProps } from './ProjectAppartments.types';

export const ProjectAppartments = (props: IProjectAppartmentsProps) => {
  return <Card>TODO:</Card>;
};
